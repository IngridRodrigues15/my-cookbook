<%= render "shared/header" %>

<h1> Resultado da busca por:  <%= @search_term %> </h1>

<% if @recipes.empty? %>
    Nenhuma receita encontrada
<% end %>

<% @recipes.each do |recipe| %>
  <%= render recipe %>
<% end %>

<%= render "shared/footer" %>
