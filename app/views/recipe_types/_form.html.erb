<%= form_for @recipe_type do |f| %>

  <%= render partial: "shared/error", locals: {zone: @recipe_type} %>

  <%= f.label :name, "Nome" %>
  <%= f.text_field :name, :required => true %>

  <%= f.submit "Enviar" %>
<% end %>